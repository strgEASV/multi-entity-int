<Project Sdk="Microsoft.NET.Sdk">

  <!-- Service vidí na Infra (potřebuje Book a MyDatabaseConnection),
       ale NEVIDÍ na API. Závislosti jdou jen jedním směrem: API -> Service -> Infra. -->
  <ItemGroup>
    <ProjectReference Include="..\Infra\Infra.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
